// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: ahand/v1/browser.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "ahand.v1";

/** BrowserRequest - cloud asks local to execute a browser command. */
export interface BrowserRequest {
  requestId: string;
  /** maps to agent-browser --session parameter */
  sessionId: string;
  /** "open", "click", "snapshot" etc. (maps directly to CLI commands) */
  action: string;
  /** extra parameters as JSON (e.g. {"selector":"@e2","value":"hello"}) */
  paramsJson: string;
  /** 0 = default 30s */
  timeoutMs: number;
}

/** BrowserResponse - result of a browser command. */
export interface BrowserResponse {
  requestId: string;
  sessionId: string;
  success: boolean;
  /** data field from agent-browser --json output */
  resultJson: string;
  error: string;
  /** screenshot/PDF binary data */
  binaryData: Buffer;
  /** MIME type for binary_data (e.g. "image/png") */
  binaryMime: string;
}

function createBaseBrowserRequest(): BrowserRequest {
  return { requestId: "", sessionId: "", action: "", paramsJson: "", timeoutMs: 0 };
}

export const BrowserRequest: MessageFns<BrowserRequest> = {
  encode(message: BrowserRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.sessionId !== "") {
      writer.uint32(18).string(message.sessionId);
    }
    if (message.action !== "") {
      writer.uint32(26).string(message.action);
    }
    if (message.paramsJson !== "") {
      writer.uint32(34).string(message.paramsJson);
    }
    if (message.timeoutMs !== 0) {
      writer.uint32(40).uint64(message.timeoutMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BrowserRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBrowserRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.action = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.paramsJson = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.timeoutMs = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BrowserRequest {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      action: isSet(object.action) ? globalThis.String(object.action) : "",
      paramsJson: isSet(object.paramsJson)
        ? globalThis.String(object.paramsJson)
        : isSet(object.params_json)
        ? globalThis.String(object.params_json)
        : "",
      timeoutMs: isSet(object.timeoutMs)
        ? globalThis.Number(object.timeoutMs)
        : isSet(object.timeout_ms)
        ? globalThis.Number(object.timeout_ms)
        : 0,
    };
  },

  toJSON(message: BrowserRequest): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.action !== "") {
      obj.action = message.action;
    }
    if (message.paramsJson !== "") {
      obj.paramsJson = message.paramsJson;
    }
    if (message.timeoutMs !== 0) {
      obj.timeoutMs = Math.round(message.timeoutMs);
    }
    return obj;
  },

  create(base?: DeepPartial<BrowserRequest>): BrowserRequest {
    return BrowserRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BrowserRequest>): BrowserRequest {
    const message = createBaseBrowserRequest();
    message.requestId = object.requestId ?? "";
    message.sessionId = object.sessionId ?? "";
    message.action = object.action ?? "";
    message.paramsJson = object.paramsJson ?? "";
    message.timeoutMs = object.timeoutMs ?? 0;
    return message;
  },
};

function createBaseBrowserResponse(): BrowserResponse {
  return {
    requestId: "",
    sessionId: "",
    success: false,
    resultJson: "",
    error: "",
    binaryData: Buffer.alloc(0),
    binaryMime: "",
  };
}

export const BrowserResponse: MessageFns<BrowserResponse> = {
  encode(message: BrowserResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.sessionId !== "") {
      writer.uint32(18).string(message.sessionId);
    }
    if (message.success !== false) {
      writer.uint32(24).bool(message.success);
    }
    if (message.resultJson !== "") {
      writer.uint32(34).string(message.resultJson);
    }
    if (message.error !== "") {
      writer.uint32(42).string(message.error);
    }
    if (message.binaryData.length !== 0) {
      writer.uint32(50).bytes(message.binaryData);
    }
    if (message.binaryMime !== "") {
      writer.uint32(58).string(message.binaryMime);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BrowserResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBrowserResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.resultJson = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.error = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.binaryData = Buffer.from(reader.bytes());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.binaryMime = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BrowserResponse {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      resultJson: isSet(object.resultJson)
        ? globalThis.String(object.resultJson)
        : isSet(object.result_json)
        ? globalThis.String(object.result_json)
        : "",
      error: isSet(object.error) ? globalThis.String(object.error) : "",
      binaryData: isSet(object.binaryData)
        ? Buffer.from(bytesFromBase64(object.binaryData))
        : isSet(object.binary_data)
        ? Buffer.from(bytesFromBase64(object.binary_data))
        : Buffer.alloc(0),
      binaryMime: isSet(object.binaryMime)
        ? globalThis.String(object.binaryMime)
        : isSet(object.binary_mime)
        ? globalThis.String(object.binary_mime)
        : "",
    };
  },

  toJSON(message: BrowserResponse): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.resultJson !== "") {
      obj.resultJson = message.resultJson;
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    if (message.binaryData.length !== 0) {
      obj.binaryData = base64FromBytes(message.binaryData);
    }
    if (message.binaryMime !== "") {
      obj.binaryMime = message.binaryMime;
    }
    return obj;
  },

  create(base?: DeepPartial<BrowserResponse>): BrowserResponse {
    return BrowserResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BrowserResponse>): BrowserResponse {
    const message = createBaseBrowserResponse();
    message.requestId = object.requestId ?? "";
    message.sessionId = object.sessionId ?? "";
    message.success = object.success ?? false;
    message.resultJson = object.resultJson ?? "";
    message.error = object.error ?? "";
    message.binaryData = object.binaryData ?? Buffer.alloc(0);
    message.binaryMime = object.binaryMime ?? "";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
