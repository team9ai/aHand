// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: ahand/v1/app_tool.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "ahand.v1";

/**
 * AppToolDescriptor - one application-defined tool advertised by the host app
 * embedding ahandd. Names are flat per device (device <-> app is 1:1).
 */
export interface AppToolDescriptor {
  /** ^[a-z0-9_-]{1,64}$ */
  name: string;
  description: string;
  /** JSON Schema, serialized JSON object */
  inputSchemaJson: string;
  /** tighten-only: forces approval in every session mode */
  requiresApproval: boolean;
}

/**
 * AppToolsUpdate - daemon -> hub. FULL snapshot of registered tools.
 * Sent on every register/unregister and re-sent after each Hello handshake.
 */
export interface AppToolsUpdate {
  /** monotonic per daemon process */
  revision: number;
  tools: AppToolDescriptor[];
}

/** AppToolRequest - hub -> daemon. Invoke one app tool. */
export interface AppToolRequest {
  toolCallId: string;
  name: string;
  /** JSON object matching input_schema_json */
  argsJson: string;
  /** daemon clamps to [1_000, 300_000]; 0 => 60_000 */
  timeoutMs: number;
}

/** AppToolError - structured failure for one invocation; code is machine-readable, message is a human-readable remediation hint. */
export interface AppToolError {
  /** TOOL_NOT_FOUND | INVALID_ARGS | SESSION_INACTIVE | APPROVAL_DENIED */
  code: string;
  /**
   * | APPROVAL_TIMEOUT | EXECUTION_TIMEOUT | HANDLER_PANIC
   * | HANDLER_ERROR | CONCURRENCY_LIMIT
   */
  message: string;
}

/** AppToolResponse - daemon -> hub. Single response per invocation (no streaming). */
export interface AppToolResponse {
  toolCallId: string;
  resultJson?: string | undefined;
  error?: AppToolError | undefined;
}

function createBaseAppToolDescriptor(): AppToolDescriptor {
  return { name: "", description: "", inputSchemaJson: "", requiresApproval: false };
}

export const AppToolDescriptor: MessageFns<AppToolDescriptor> = {
  encode(message: AppToolDescriptor, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.inputSchemaJson !== "") {
      writer.uint32(26).string(message.inputSchemaJson);
    }
    if (message.requiresApproval !== false) {
      writer.uint32(32).bool(message.requiresApproval);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppToolDescriptor {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppToolDescriptor();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.inputSchemaJson = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.requiresApproval = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppToolDescriptor {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      inputSchemaJson: isSet(object.inputSchemaJson)
        ? globalThis.String(object.inputSchemaJson)
        : isSet(object.input_schema_json)
        ? globalThis.String(object.input_schema_json)
        : "",
      requiresApproval: isSet(object.requiresApproval)
        ? globalThis.Boolean(object.requiresApproval)
        : isSet(object.requires_approval)
        ? globalThis.Boolean(object.requires_approval)
        : false,
    };
  },

  toJSON(message: AppToolDescriptor): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.inputSchemaJson !== "") {
      obj.inputSchemaJson = message.inputSchemaJson;
    }
    if (message.requiresApproval !== false) {
      obj.requiresApproval = message.requiresApproval;
    }
    return obj;
  },

  create(base?: DeepPartial<AppToolDescriptor>): AppToolDescriptor {
    return AppToolDescriptor.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AppToolDescriptor>): AppToolDescriptor {
    const message = createBaseAppToolDescriptor();
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.inputSchemaJson = object.inputSchemaJson ?? "";
    message.requiresApproval = object.requiresApproval ?? false;
    return message;
  },
};

function createBaseAppToolsUpdate(): AppToolsUpdate {
  return { revision: 0, tools: [] };
}

export const AppToolsUpdate: MessageFns<AppToolsUpdate> = {
  encode(message: AppToolsUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.revision !== 0) {
      writer.uint32(8).uint64(message.revision);
    }
    for (const v of message.tools) {
      AppToolDescriptor.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppToolsUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppToolsUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.revision = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tools.push(AppToolDescriptor.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppToolsUpdate {
    return {
      revision: isSet(object.revision) ? globalThis.Number(object.revision) : 0,
      tools: globalThis.Array.isArray(object?.tools) ? object.tools.map((e: any) => AppToolDescriptor.fromJSON(e)) : [],
    };
  },

  toJSON(message: AppToolsUpdate): unknown {
    const obj: any = {};
    if (message.revision !== 0) {
      obj.revision = Math.round(message.revision);
    }
    if (message.tools?.length) {
      obj.tools = message.tools.map((e) => AppToolDescriptor.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<AppToolsUpdate>): AppToolsUpdate {
    return AppToolsUpdate.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AppToolsUpdate>): AppToolsUpdate {
    const message = createBaseAppToolsUpdate();
    message.revision = object.revision ?? 0;
    message.tools = object.tools?.map((e) => AppToolDescriptor.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAppToolRequest(): AppToolRequest {
  return { toolCallId: "", name: "", argsJson: "", timeoutMs: 0 };
}

export const AppToolRequest: MessageFns<AppToolRequest> = {
  encode(message: AppToolRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.toolCallId !== "") {
      writer.uint32(10).string(message.toolCallId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.argsJson !== "") {
      writer.uint32(26).string(message.argsJson);
    }
    if (message.timeoutMs !== 0) {
      writer.uint32(32).uint32(message.timeoutMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppToolRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppToolRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.toolCallId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.argsJson = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.timeoutMs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppToolRequest {
    return {
      toolCallId: isSet(object.toolCallId)
        ? globalThis.String(object.toolCallId)
        : isSet(object.tool_call_id)
        ? globalThis.String(object.tool_call_id)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      argsJson: isSet(object.argsJson)
        ? globalThis.String(object.argsJson)
        : isSet(object.args_json)
        ? globalThis.String(object.args_json)
        : "",
      timeoutMs: isSet(object.timeoutMs)
        ? globalThis.Number(object.timeoutMs)
        : isSet(object.timeout_ms)
        ? globalThis.Number(object.timeout_ms)
        : 0,
    };
  },

  toJSON(message: AppToolRequest): unknown {
    const obj: any = {};
    if (message.toolCallId !== "") {
      obj.toolCallId = message.toolCallId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.argsJson !== "") {
      obj.argsJson = message.argsJson;
    }
    if (message.timeoutMs !== 0) {
      obj.timeoutMs = Math.round(message.timeoutMs);
    }
    return obj;
  },

  create(base?: DeepPartial<AppToolRequest>): AppToolRequest {
    return AppToolRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AppToolRequest>): AppToolRequest {
    const message = createBaseAppToolRequest();
    message.toolCallId = object.toolCallId ?? "";
    message.name = object.name ?? "";
    message.argsJson = object.argsJson ?? "";
    message.timeoutMs = object.timeoutMs ?? 0;
    return message;
  },
};

function createBaseAppToolError(): AppToolError {
  return { code: "", message: "" };
}

export const AppToolError: MessageFns<AppToolError> = {
  encode(message: AppToolError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppToolError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppToolError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppToolError {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: AppToolError): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<AppToolError>): AppToolError {
    return AppToolError.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AppToolError>): AppToolError {
    const message = createBaseAppToolError();
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseAppToolResponse(): AppToolResponse {
  return { toolCallId: "", resultJson: undefined, error: undefined };
}

export const AppToolResponse: MessageFns<AppToolResponse> = {
  encode(message: AppToolResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.toolCallId !== "") {
      writer.uint32(10).string(message.toolCallId);
    }
    if (message.resultJson !== undefined) {
      writer.uint32(18).string(message.resultJson);
    }
    if (message.error !== undefined) {
      AppToolError.encode(message.error, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppToolResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppToolResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.toolCallId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.resultJson = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.error = AppToolError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppToolResponse {
    return {
      toolCallId: isSet(object.toolCallId)
        ? globalThis.String(object.toolCallId)
        : isSet(object.tool_call_id)
        ? globalThis.String(object.tool_call_id)
        : "",
      resultJson: isSet(object.resultJson)
        ? globalThis.String(object.resultJson)
        : isSet(object.result_json)
        ? globalThis.String(object.result_json)
        : undefined,
      error: isSet(object.error) ? AppToolError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: AppToolResponse): unknown {
    const obj: any = {};
    if (message.toolCallId !== "") {
      obj.toolCallId = message.toolCallId;
    }
    if (message.resultJson !== undefined) {
      obj.resultJson = message.resultJson;
    }
    if (message.error !== undefined) {
      obj.error = AppToolError.toJSON(message.error);
    }
    return obj;
  },

  create(base?: DeepPartial<AppToolResponse>): AppToolResponse {
    return AppToolResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AppToolResponse>): AppToolResponse {
    const message = createBaseAppToolResponse();
    message.toolCallId = object.toolCallId ?? "";
    message.resultJson = object.resultJson ?? undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? AppToolError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
