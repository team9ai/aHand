export default function DashboardHomePage() {
  return (
    <section>
      <h2 className="dashboard-title">Dashboard Ready</h2>
      <p className="dashboard-copy">
        You are signed in through the shared hub auth shell. Task 8 will replace this landing
        view with live devices, jobs, and audit surfaces.
      </p>
    </section>
  );
}
